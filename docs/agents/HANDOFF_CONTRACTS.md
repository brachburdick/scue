# Legacy Compatibility Pointer

The canonical artifact schemas for SCUE now live in:

- `templates/`
- `/Users/brach/Documents/THE_FACTORY/OPERATOR_PROTOCOL.md`

Use the project templates as the working copies and the root operator protocol as the schema reference. This legacy note remains only so older references do not break.
